apiVersion: apps/v1
kind: Deployment
metadata:
  name: it-automation-backyard
  labels:
    app: it-automation-backyard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: it-automation-backyard
  template:
    metadata:
      labels:
        app: it-automation-backyard
    spec:
      containers:
        - name: {{ include "ita.backyard.containerName" . }}
          image: {{ include "ita.backyard.image" . }}
          envFrom:
            - configMapRef:
                name: it-automation-cm
            - secretRef:
                name: it-automation-secret
          ports:
            - containerPort: 80
          securityContext:
            privileged: true
          volumeMounts:
            - name: exastro-file-volume
              mountPath: /exastro-file-volume
      volumes:
        - name: exastro-file-volume
          persistentVolumeClaim:
            claimName: exastro-file-pvc