apiVersion: apps/v1
kind: Deployment
metadata:
  name: it-automation-ansible
  labels:
    app: it-automation-ansible
spec:
  replicas: 1
  selector:
    matchLabels:
      app: it-automation-ansible
  template:
    metadata:
      labels:
        app: it-automation-ansible
    spec:
      containers:
        - name: {{ include "ita.ansible.containerName" . }}
          image: {{ include "ita.ansible.image" . }}
          ports:
            - containerPort: 80
            - containerPort: 443
          securityContext:
            privileged: true
          volumeMounts:
            - name: exastro-file-volume
              mountPath: /exastro-file-volume
      volumes:
        - name: exastro-file-volume
          persistentVolumeClaim:
            claimName: exastro-file-pvc